{
  "name": "gsc-slack-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "login": "wrangler login",
    "whoami": "wrangler whoami",
    "kv:create": "wrangler kv namespace create GSC_CACHE && wrangler kv namespace create GSC_CACHE --preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.103.0"
  }
}
